/* Counters */
int i;
int retrain;
int numRetrain;
int numLastRetrained;
int JustTrained;
int num_pixels;
int x;
int y;
int x_retrain;
int y_retrain;
int best_x;
int best_y;
float confidence;
float best_confidence;
float confidenceLevel;
float scale_zoom;
float scale_before;
int out_of_scene;
float scale_to_retrain;
float max_scale;
float min_scale;
int num_frames;

i = 0;
/* Trainning */
#Treinando com a imagem 0
GetImage(i);
MoveToTargetCenter(0);
key t;
ForwardVisualSearchNetwork(0);
#forward outputs;

/* Testing */
numLastRetrained = -1;
i = i + 1;
GetImage(i);
MoveToTargetCenter(0);
JustTrained = 1;

max_scale = GetMaxScale(0);
min_scale = GetMinScale(0);

num_frames = GetNumFrames(0);

####
key s;
pause;

/* Testando */
GetImage(665);
SetScaleFactor(1.234546);
ForwardVisualSearchNetwork(0);

MoveToPoint(282,233);
MoveToPoint(282,233);
pause;

####

/* Testando */
GetImage(1331);
SetScaleFactor(0.576614);
ForwardVisualSearchNetwork(0);

MoveToPoint(339,74);
MoveToPoint(339,74);
pause;

####

/* Testando */
GetImage(1997);
SetScaleFactor(0.452008);
ForwardVisualSearchNetwork(0);

MoveToPoint(158,266);
MoveToPoint(158,266);
pause;

####

/* Testando */
GetImage(2664);
SetScaleFactor(0.387071);
ForwardVisualSearchNetwork(0);

MoveToPoint(171,95);
MoveToPoint(171,95);
pause;




####

###
DisposeMemory(0);
quit;



